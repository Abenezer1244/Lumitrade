# Environment files — NEVER commit
.env
.env.local
.env.*.local
.env.production
.env.staging
*.env

# Key files
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519

# Secrets and credentials
secrets/
credentials/
*credentials*.json
*service-account*.json

# Log files (may contain sensitive data)
logs/
*.log

# Python
__pycache__/
.venv/
venv/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage

# Node
node_modules/
.next/
out/

# OS
.DS_Store
Thumbs.db
Desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Docker
docker-compose.override.yml

# Supabase local
supabase/.temp/
.claude/skills/gstack/

# Tool runtime state — large, never review-relevant
.claude-flow/
.claude/*
!.claude/settings.json
# .claude/settings.local.json is DELIBERATELY NOT allowlisted — Claude's `.local`
# convention means per-machine secrets/permissions, never to be committed.
# Codex review 2026-04-25 flagged 34 JWT tokens in this file when it was
# allowlisted; treating it as gitignored at all times closes that hole.
.firecrawl/
.gstack/
.superpowers/
.swarm/
.obsidian/

# Large local-only data
graphify-out/
backend/data/

# Plugin / tool config (per-machine, not for review)
.mcp.json
skills-lock.json

# Shell-quoting artifacts (created by accidental bash splits — clean up if seen)
$*
[0-9]
[0-9][0-9]
